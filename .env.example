# 当前版本默认使用本地 mock provider，不需要 API Key。
# VITE_* 变量会进入浏览器构建产物，禁止在这里填写任何密钥。

VITE_AI_PROVIDER=mock
VITE_RESOURCE_PROVIDER=mock

# 未来接入服务端 provider 时使用服务端环境变量，不要把私密地址或 Token 暴露到 VITE_*。
# VITE_AI_API_BASE_URL=
