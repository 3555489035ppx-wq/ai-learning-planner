# 截图清单

自动化视觉回归会输出到 `docs/screenshots/after/`：

- 桌面端：`landing`、`onboarding`、`diagnosis`、`plan`、`today`、`resources`、`progress`、`weekly-review`、`settings`、`plan-recovery`。
- 移动端：`landing`、`today`、`diagnosis`、`plan`、`weekly-review`、`settings`。

分辨率分别为 `1440x900` 与 `390x844`。每轮截图前均注入“高中数学 9/100、低置信度诊断与恢复信号”演示数据；不修改默认空白用户数据。
