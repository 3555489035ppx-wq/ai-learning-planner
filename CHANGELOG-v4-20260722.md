# 假期跃迁 v4 整改记录

日期：2026-07-22

## 产品与数据逻辑

- 将首次规划改为课程、目标、现实安排三类完整输入，数据返回上一步和刷新后保留。
- 建立 109 门稳定课程身份、别名归一化与歧义确认；课程诊断、任务、资源、进展按 `courseId` 隔离。
- 支持用户课程添加、编辑、暂停、恢复、删除、撤销和重复拦截。
- 计划生成改为“生成草案 → 展示依据与校验 → 用户确认 → 原子启用”，不再用页面按钮直接覆盖当前计划。
- 全局排程遵守假期边界、旅行日、休息日、可用时段、每日容量、最长专注时间、阶段边界与 4＋4＋2 节次。
- 90% 利用率与课程相邻节次作为软偏好处理；容量、日期、旅行与休息约束保持硬校验。
- 修改成绩只使对应课程诊断待复核；修改时间与目标只使计划过期；新计划保留已完成任务。
- 今日任务支持开始、完成、自检、实际时长、延后、跳过、删除和资源切换，所有变化写入事件并持久化。
- 学习进展只基于真实可比数据计算，区分无数据、单时间点和趋势；周复盘真实应用下一周计划并支持撤销。

## 学习资源

- 页面统一通过 `ResourceProvider` 读取人工审核目录、用户链接和搜索候选。
- 真实资源保留平台、作者、链接、BV 号、审核状态、知识点与适用任务；外链打开前进行离站确认。
- 用户可把资源加入指定课程、知识点、日期与节次，也可替换或标记不感兴趣。
- 搜索候选只提供原平台检索入口，不伪造标题、作者、热度或视频元数据。
- B 站热度没有带日期快照时保持 `null`，不以 0 冒充真实值。

## UI/UX

- 新建并遵守根目录 `DESIGN.md`，统一颜色、排版、间距、组件、动效、无障碍和真实性规则。
- 首页改为安静的产品入口，示例与真实计划明确区分；返回用户可直接继续今日计划。
- 应用区使用紧凑工作台结构，减少大蓝色块、装饰图表、卡片堆叠和无意义顶部留白。
- 增加项目内 `/design-system` 页面，覆盖按钮、标签页、通知、表单、错误、空状态、弹窗和 Toast 状态。
- 所有核心交互具备 hover、active、focus-visible、loading、success、error 与 disabled 反馈。
- 移动端使用底部导航或抽屉，390px 无横向溢出；支持 reduced-motion、reduced-transparency 与高对比偏好。

## Apple Design Skill 的实际影响

- 即时按压反馈与可见焦点，不用花哨长动画掩盖响应。
- 计划覆盖、课程删除、资源离站和周复盘应用均先确认，并提供撤销或版本恢复。
- 动效保持可中断、进出对称并遵守 `prefers-reduced-motion`。
- 直接操控与键盘路径保持等价；信息层级优先于玻璃、光效或拟物装饰。

## 质量与证据

- 81 项单元/领域测试覆盖课程库、迁移、排程、计划版本、资源真实性、今日执行、进展和周复盘。
- 13 项 Playwright 流程覆盖桌面端、1024px 和 390px；最终退出码为 0。
- 最终截图位于 `docs/screenshots/after/`。
- 资源覆盖与剩余限制分别记录在 `docs/resource-coverage-v4.md` 和 `docs/remaining-limitations.md`。
