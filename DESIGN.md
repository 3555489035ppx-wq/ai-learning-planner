# 假期跃迁设计规范

本文件是“假期跃迁”的设计语义来源。项目使用 Apple Design 的交互原则——即时反馈、可预测、可撤销、可访问——但不复制 Apple 官网或系统界面。

## 产品气质

- 定位：安静、专业、可信、可解释的 Personal Learning System。
- 核心感受：学习者应感到被帮助做出取舍，而不是被一个自动生成器催促。
- 信息优先级：学习事实 → 下一步行动 → 安排依据 → 高级操作。
- 蓝色只用于主操作、选中、焦点和关键链接；不作为大面积背景。

禁止使用：蓝紫渐变、发光按钮、机器人、AI 星星、魔法棒、装饰性聊天气泡、排行榜、连续打卡火焰、无意义图表和卡片墙。

## 设计 Token

所有颜色从 `tokens.css` 引用；页面和组件中不得临时写入颜色值。

| Token | 用途 |
|---|---|
| Canvas `#F5F5F7` | 页面背景 |
| Surface `#FFFFFF` | 表面与表单 |
| Ink `#172033` | 主文字 |
| Muted `#66758A` | 次级信息 |
| Action `#1769E0` | 主操作、链接、焦点 |
| Tint `#EAF2FF` | 选中和轻提示 |
| Border `#DDE4ED` | 分隔和边界 |
| Success `#187A52` | 已完成与成功 |
| Warning `#956000` | 需要关注 |
| Danger `#B93746` | 破坏性操作和错误 |

## 排版与布局

- 使用系统中文 UI 字体栈，不加载或伪装使用 SF Pro。
- 正文不低于 16px；交互文字不低于 15px；辅助信息不低于 13px。
- 大标题使用紧凑行高和轻微负字距；正文使用 1.55–1.7 行高。
- 数字、分数和时长使用等宽数字。
- 页面主内容最大宽度约 1200px；长文最大宽度约 720px；移动端边距至少 16px。
- 留白必须建立层级，连续无意义空白不得超过约 160px。
- 组件最小触控高度为 44px。

## 组件语义

- 每屏最多一个视觉主按钮；次级操作使用描边或文字按钮。
- 表单必须有可见标签、帮助文本和就近错误提示；错误不能只靠颜色表达。
- 图标按钮必须有 `aria-label` 与 Tooltip。
- 危险操作必须在确认语境中执行，或提供明确撤销。
- 空状态必须说明原因与下一步；不展示“空白但像损坏”的界面。
- 长文本容器必须允许换行或 `line-clamp`；Flex/Grid 子项使用 `min-width: 0`。

## Apple Design 交互原则

- 按钮在 pointer-down / active 时即时反馈：`scale(0.98)`，再在点击后提交行为。
- 所有计划调整先预览，再确认；成功后提供 Toast、ChangeSet 或撤销。
- 任务拖动保持直接操控；键盘和触控必须有等价路径。
- Modal、Drawer 和 Sheet 可随时关闭；进入和退出路径对称。
- 动画仅使用 `transform` 与 `opacity`，不使用 `transition: all`。
- 常规时长：160–220ms；Dialog/Sheet/Toast 不超过 240ms；曲线为 `cubic-bezier(0.2, 0.8, 0.2, 1)`。
- `prefers-reduced-motion: reduce` 时移除位移、弹跳和旋转，仅保留短淡入淡出或静态反馈。
- `prefers-reduced-transparency` 和更高对比度偏好下，降低透明和模糊，增强边界。

## 可访问性与表单

- 使用语义化 `button`、`a`、`label`、`fieldset`、`dialog` 语义；不要用普通 `div` 充当按钮。
- 页面提供跳到主内容链接；路由切换后主标题获取焦点。
- 所有可交互元素有清晰 `:focus-visible` 焦点环。
- Toast 与异步状态使用 `aria-live="polite"`。
- Modal 支持 Escape、焦点循环、背景滚动锁定和关闭后焦点恢复。
- 输入使用正确 `type`、`name` 和 `autocomplete`；不阻止粘贴。
- 禁用按钮必须有可见原因；提交按钮在异步开始前保持可点，开始后防止重复提交。

## 页面约束

- 首页只做产品认知、价值与开始使用；不承担大型案例展示。
- 诊断展示成绩事实、置信度、证据边界和阶段路径，不使用装饰性雷达图。
- 计划页区分阶段、Task Pool、周课表和 backlog；backlog 不是错误。
- 今日页只突出下一件事，同时保留解释、完成标准和实际时长记录。
- 进展与周复盘只基于真实任务、测验和反思证据；证据不足时不制造趋势。
- 资源页必须区分人工审核、用户提供和搜索候选。

## 内容诚实边界

- 本地规则 provider 必须明确标记为本地规则或模拟服务，不暗示已连接远程 AI。
- 不伪造成绩提升、用户研究结论、资源热度、视频元数据或课程章节。
- 建议必须附带“为什么这样安排”和必要的限制说明；不保证学习结果。

## 维护规则

- `tokens.css` 是 Token 实现来源；本文件是设计语义来源。
- `src/index.css` 保留基础样式，新增全站视觉规则集中在 `src/v4.css`。
- 新组件优先复用既有 Button、Modal、Notice、Toast 与表单原语。
- 每次新增交互都需要检查：默认、hover、active、focus-visible、disabled、loading、success、error、empty、移动端和 reduced-motion。
